package desktop

/**
 * Human-readable build stamp shown in Settings so a user can tell us which
 * release they're actually running (the release exe's version stays 0.1.0).
 * Bump DATE whenever a new build is published.
 */
object Build {
    const val VERSION = "0.1.0"
    const val DATE = "2026-08-21"
}
